machine:
    node:
        version: v0.10
dependencies:
    pre:
        - "npm install"
    cache_directories:
        - "~/.npm"
    override:
        - npm -g install npm@latest
checkout:
    post:
        - git checkout 1.0.1-alpha